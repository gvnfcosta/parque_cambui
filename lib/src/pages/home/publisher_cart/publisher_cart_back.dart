import 'package:flutter/material.dart';

class PublisherCartBack extends StatelessWidget {
  const PublisherCartBack({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
